# Slash commands do projeto

Automações específicas deste repo (RFC-0001 §5.2.2). Nenhuma ainda — crie um arquivo
`.md` aqui quando um fluxo se repetir (ex.: `scaffold-source.md`, `seed-catalog.md`).
